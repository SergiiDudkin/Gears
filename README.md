# Build
Go to the directory with ```pyproject.toml``` file and run
```
python -m build
```

# Installation
Go to the directory with ```pyproject.toml``` file and run
```
python -m pip install "dist/gears-0.1.0.tar.gz"
```
